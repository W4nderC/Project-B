using UnityEngine;

public class MaleNpc : MonoBehaviour, INpc 
{
    public void GetShot()
    {
        print("Npc is shot");
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
